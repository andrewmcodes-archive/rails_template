<p id="notice"><%= notice %></p>

<div class="relative bg-indigo-900 md:pt-32 pb-32 pt-12 px-4 md:px-10 mx-auto">
    <h1 class="font-semibold text-4xl text-gray-100 my-8">Posts</h1>
</div>

<div class="px-4 md:px-10 mx-auto w-full -m-24">
  <div class="relative flex flex-col min-w-0 break-words bg-white w-full mb-6 shadow-lg rounded">
    <table class="items-center w-full bg-transparent border-collapse shadow-xl">
      <thead>
        <tr>
          <th class="table-header">Title</th>
          <th class="table-header">Content</th>
          <th class="table-header" colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% @posts.each do |post| %>
          <tr>
            <td class="table-content"><%= post.title %></td>
            <td class="table-content"><%= post.content %></td>
            <td class="table-content"><%= link_to "Show", post %></td>
            <td class="table-content"><%= link_to "Edit", edit_post_path(post) %></td>
            <td class="table-content"><%= link_to "Destroy", post, method: :delete, data: {confirm: "Are you sure?"} %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</button>
  <%= link_to "New Post", new_post_path, class: "bg-transparent hover:bg-indigo-900 text-indigo-700 font-semibold hover:text-white py-2 px-4 border border-indigo-900 hover:border-transparent rounded" %>
</div>
